#!/bin/sh
JMETER_HOME=${JMETER_HOME:=/usr/lib/jmeter}

if [ ${LANG:0:2} = "ja" ]; then
    JVM_ARGS="-Duser.language=ja"
else
    JVM_ARGS=""
fi

JMETER_HOME=$JMETER_HOME \
	JVM_ARGS=$JVM_ARGS \
	$JMETER_HOME/bin/jmeter "$@"
