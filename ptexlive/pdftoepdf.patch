diff -uNr texlive-20091107-source.orig//texk/web2c/pdftexdir/pdftoepdf.cc texlive-20091107-source//texk/web2c/pdftexdir/pdftoepdf.cc
--- texlive-20091107-source.orig//texk/web2c/pdftexdir/pdftoepdf.cc	2009-05-06 17:19:56.000000000 +0900
+++ texlive-20091107-source//texk/web2c/pdftexdir/pdftoepdf.cc	2011-02-22 21:17:35.000000000 +0900
@@ -650,7 +650,7 @@
     }
     for (r = encodingList; r != 0; r = n) {
         n = r->next;
-        delete r->font;
+//        delete r->font;
         delete r;
     }
 }
@@ -690,6 +690,7 @@
     Page *page;
     int rotate;
     PDFRectangle *pagebox;
+    int minor_pdf_version_found, major_pdf_version_found;
     float pdf_version_found, pdf_version_wanted;
     // initialize
     if (!isInit) {
@@ -705,8 +706,11 @@
     // this works only for PDF 1.x -- but since any versions of PDF newer
     // than 1.x will not be backwards compatible to PDF 1.x, pdfTeX will
     // then have to changed drastically anyway.
-    pdf_version_found = pdf_doc->doc->getPDFVersion();
+    minor_pdf_version_found = pdf_doc->doc->getPDFMinorVersion();
+    major_pdf_version_found = pdf_doc->doc->getPDFMajorVersion();
+    pdf_version_found = major_pdf_version_found + (minor_pdf_version_found * 0.1);
     pdf_version_wanted = 1 + (minor_pdf_version_wanted * 0.1);
+
     if (pdf_version_found > pdf_version_wanted) {
         char msg[] =
             "PDF inclusion: found PDF version <%.1f>, but at most version <%.1f> allowed";
diff -uNr texlive-20091107-source.orig//texk/xdvik/configure texlive-20091107-source//texk/xdvik/configure
--- texlive-20091107-source.orig//texk/xdvik/configure	2009-06-24 01:30:37.000000000 +0900
+++ texlive-20091107-source//texk/xdvik/configure	2011-02-22 21:18:07.000000000 +0900
@@ -23477,7 +23477,7 @@
 else
   { { $as_echo "$as_me:$LINENO: error: your kpathsea has no kpse_enc_format." >&5
 $as_echo "$as_me: error: your kpathsea has no kpse_enc_format." >&2;}
-   { (exit 1); exit 1; }; }
+  }
 fi
 
 
